import React from "react";
import { SafeAreaView } from "react-native";
import UploadServiceTag from "./uploadservicetag";

const App = () => {
    return (
        <SafeAreaView style={{ flex: 1 }}>
            <UploadServiceTag />
        </SafeAreaView>
    );
};

export default App;
